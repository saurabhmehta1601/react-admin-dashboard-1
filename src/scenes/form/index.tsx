import Header from '../../components/Header'

const Form = () => {
  return (
    <div>
      <Header heading='CREATE USER' subHeading='Create a new user profile' />
    </div>
  )
}

export default Form